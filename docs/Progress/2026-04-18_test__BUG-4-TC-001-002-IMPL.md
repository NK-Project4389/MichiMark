# BUG-4 テストコード実装（T-501b）

## 完了した作業

### テストコード実装
- **ファイル**: `flutter/integration_test/bug4_invite_navigation_test.dart`（新規作成）
- **テストシナリオ**: TC-BUG4-001〜002（3件）

### 実装内容

#### TC-BUG4-001: 招待ボタン遷移先確認
- 前提: イベント詳細画面を開き、ownerとして「メンバーを招待」ボタンを表示する
- 操作: 「メンバーを招待」ボタンをタップ
- 期待結果: 招待リンク共有画面（BottomSheet）が表示される
  - `Key('inviteLinkShare_sheet_root')` が存在（招待リンク共有画面）
  - `Key('invite_code_text_field')` が存在しない（招待コード入力画面ではない）

#### TC-BUG4-001b: 招待リンク共有画面の構成確認
- 前提: TC-BUG4-001と同じ
- 期待結果: 招待リンク共有画面に「招待リンクを作成」ボタンが表示される
  - `Key('inviteLinkShare_button_create')` が存在
  - 招待コード入力画面のテキストフィールドが表示されていない

#### TC-BUG4-002: BottomSheet閉鎖テスト
- 前提: 招待リンク共有BottomSheetが表示されている
- 操作: BottomSheet背景をタップして閉じる
- 期待結果: BottomSheetが閉じてイベント詳細画面に戻る
  - `Key('inviteLinkShare_sheet_root')` が存在しない
  - 概要タブが表示される

### テストコード品質

#### ルール準拠状況
- ✅ `pumpAndSettle()` 禁止: すべて `pump(Duration(...))` で固定時間待機
- ✅ 各テストで個別に `GetIt.I.reset()` → `router.go('/')` → `app.main()` を呼び出し
- ✅ `setUpAll` でアプリ起動をしない（2回目テスト全滅バグ対策）
- ✅ スクロール必要な要素は `tester.ensureVisible()` を使用
- ✅ SKIP ガード実装：イベントがない場合・ownerロール未設定の場合はSKIP

#### 前提条件
- iOSシミュレーター起動済み
- シードデータにイベント1件以上
- ユーザーが owner 権限（StubInvitationRepository.fetchUserRole で owner を返す）
- BUG-5修正済み（userRole セット・「メンバーを招待」ボタン表示）
- INV-4実装済み（招待リンク共有画面・InviteLinkShareBloc）

### コミット
- コミットハッシュ: `e8daf96`
- コミットメッセージ: `test: BUG-4 招待ボタン遷移先テストコード実装（TC-BUG4-001〜002）`

## 未完了

- **テスト実行**: reviewer承認後に実施（T-503）
- **git push**: reviewerレビュー完了後に実施

## 次回セッションで最初にやること

1. **reviewer の確認待ち**
   - テストコードの実装内容・品質確認
   - Specシナリオとの整合性確認
   - ブロッキング要因がないか確認

2. **reviewer 承認後（次のセッション）**
   - テスト実行（T-503）
   - テスト結果報告・問題が出た場合は flutter-dev に引き継ぎ
   - TASKBOARD.md 更新（T-501b → DONE）
   - git push

## 参考

- **Spec 参照**: `docs/Spec/Features/FS-invitation_link_share.md` § 20（テストシナリオ）
- **テストファイル位置**: `/Users/kurosakinobuyuki/ClaudeCode/App/MichiMark/flutter/integration_test/bug4_invite_navigation_test.dart`
- **既存テストファイル（参考）**:
  - `invite_link_share_test.dart`（INV-4のテストコード）
  - `invite_code_input_test.dart`（INV-3のテストコード）

## 注記

BUG-4はBUG-5の修正に依存していた。BUG-5（userRole セット）が修正完了したため、BUG-4のテストコード実装を実施。
招待ボタンが正しく InviteLinkShare 画面（BottomSheet）に遷移することを確認するテストを設計した。
