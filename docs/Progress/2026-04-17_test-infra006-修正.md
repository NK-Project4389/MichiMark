# 2026-04-17 テストコード軽微修正（TC-INFRA-006）

## 作業内容
TC-INFRA-006のテストコードに、`migrate()` 呼び出し前に `isMigrationNeeded()` が `true` を返すことを確認する行を追加しました。

## 修正ファイル
- `/flutter/test/infra/firebase_infra_test.dart`

## テスト結果
### Unit テスト（Firebase基盤）
全8件（SKIP 1件含む）中 **7件PASS**

| シナリオID | テスト名 | 結果 |
|---|---|---|
| TC-INFRA-001 | Anonymous AuthでUIDが発行される | ✅ PASS |
| TC-INFRA-002 | 既存UIDがある場合は再発行しない | ✅ PASS |
| TC-INFRA-003 | Apple Sign InでAnonymousアカウントにリンクできる | ⏭️ SKIP |
| TC-INFRA-004 | Firestoreにメンバーを保存・取得できる | ✅ PASS |
| TC-INFRA-005 | Firestoreにイベントを保存・取得できる | ✅ PASS |
| TC-INFRA-006 | driftからFirestoreへのデータ移行が完了する | ✅ PASS |
| TC-INFRA-007 | 移行完了後にschemaVersionが更新される | ✅ PASS |
| TC-INFRA-008 | 移行済み状態ではFirestoreから読み込む | ✅ PASS |

## 修正内容詳細
```dart
// 移行前はisMigrationNeededがtrue
expect(await migrationRepository.isMigrationNeeded(), isTrue);
```

TC-INFRA-006 の `migrate()` 呼び出し前にこのアサーションを追加し、移行前の状態で `isMigrationNeeded()` が正しく `true` を返すことを明示的に検証するようにしました。

## 次回セッションで最初にやること
なし（本作業で完了）
