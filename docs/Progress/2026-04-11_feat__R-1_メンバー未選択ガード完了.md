# 2026-04-11 R-1 メンバー未選択ガード 完了

## 完了した作業

### R-1: メンバー未選択時の入力ガード（T-170〜T-174）

- **要件書**: `docs/Requirements/REQ-member_required_guard.md`
- **Spec**: `docs/Spec/Features/MemberRequiredGuard_Spec.md`
- **実装**: 4画面の `_SelectionRow` に `enabled` パラメータ追加
  - `basic_info_view.dart`: ガソリン支払者 → `draft.selectedMembers.isNotEmpty`
  - `mark_detail_page.dart`: 参加メンバー → `state.availableMembers.isNotEmpty`
  - `link_detail_page.dart`: 参加メンバー → `state.availableMembers.isNotEmpty`
  - `payment_detail_page.dart`: 支払い者・割り勘メンバー → `state.availableMembers.isNotEmpty`
- **テスト**: TC-MRG-001〜006 / **3PASS / 3SKIP / 0FAIL**
  - SKIP: TC-MRG-001/003/005（メンバー0件のシードデータなし）

### バグ修正: Slidable 排他制御

- `SlidableAutoCloseBehavior` で3画面をラップ（EventList / MichiInfo / PaymentInfo）
- 複数スワイプが同時に開く問題を修正

---

## 未完了・次回やること

1. **F-1 カスタム数値キーパッド**（タスクボードに追加済み T-180〜T-189）→ T-181 Spec作成 IN_PROGRESS
2. **TC-MRG-001/003/005**: メンバー0件の空状態UIテスト（専用シードデータ追加が必要）
3. **TestFlight アップロード**（R-1 + Slidable修正を含めたビルド）

---

## Integration Test 累計

| グループ | 件数 |
|---|---|
| TC-MRG（メンバー未選択ガード） | 3PASS / 3SKIP |
| **全体合計（累計）** | **62件（PASS+SKIP）** |
