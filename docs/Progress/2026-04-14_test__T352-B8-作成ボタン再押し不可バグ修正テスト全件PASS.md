# T-352 B-8 作成ボタン再押し不可バグ修正 Integration Test 全件PASS

## 日付
2026-04-14

## 完了した作業

### T-352: B-8 作成ボタン再押し不可バグ修正 テスト実行

- テストファイル: `flutter/integration_test/creation_button_repress_test.dart`
- 結果: **4PASS / 0FAIL / 0SKIP**

#### テスト結果

| シナリオID | テスト名 | 結果 |
|---|---|---|
| TC-CAB-001 | FABを押してインジケータータップ後にキャンセルするとFABが再度押せること | PASS |
| TC-CAB-001b | キャンセル後のFABが再タップ可能であること（isInsertModeがfalseにリセットされる） | PASS |
| TC-CAB-002 | FABを押してinsertModeに入った後にFABの×でキャンセルするとFABが再度押せること | PASS |
| TC-CAB-002b | insertModeキャンセル後にFABが再タップ可能でボトムシートが表示されること | PASS |

#### テストコード修正内容

初回テスト実行時に3件FAILが発生したため、テストフローを実装の実際の動作に合わせて修正した。

**修正前の誤り:**
- FABタップ後に直接ボトムシート（`michiInfo_button_addMark`）が表示されることを期待していた

**実際の動作（Spec準拠）:**
- FABタップ → `isInsertMode: true` → インジケーターが表示される
- インジケータータップ → `pendingInsertAfterSeq` が設定される → BlocListenerがボトムシートを表示する

**修正後のフロー:**
1. FABタップ → `michiInfo_button_insertIndicator_head` が表示されることを確認
2. インジケータータップ → ボトムシート（addMark/addLink）が表示されることを確認
3. バリアタップ or FABの×でキャンセル → isInsertMode が false にリセットされる
4. 再度FABタップ → 同フローでボトムシートが再表示されることを確認

ログ: `docs/TestLogs/2026-04-14_09-05_creation_button_repress.log`

## 更新したファイル

- `flutter/integration_test/creation_button_repress_test.dart`（テストフロー修正）
- `docs/Tasks/TASKBOARD.md`（T-352 DONE 更新）

## 次回セッションで最初にやること

- T-355: UI-12 未保存新規イベント自動削除 レビュー（reviewer）
- T-356: UI-12 未保存新規イベント自動削除 テスト実行（tester）
- T-359: UI-13 Detail画面キャンセル確認 レビュー（reviewer）
- T-360: UI-13 Detail画面キャンセル確認 テスト実行（tester）
