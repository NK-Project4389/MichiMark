# 進捗記録: INFRA-1着手・B-16完了

日付: 2026-04-15

## 完了した作業

### INFRA-1: Firebase基盤整備 Spec作成（T-345）
- `docs/Spec/Features/FS-firebase_infra.md` 作成
- 設計決定:
  - 環境分離: `--dart-define=FLAVOR=dev/prod`（Flavorなし）
  - Auth: Anonymous Auth自動発行 → Apple Sign In任意リンク（UID不変）
  - Firestore構造: `users/{uid}/events/{eventId}/` サブコレクション
  - 中間テーブル → Arrayフィールドに変換（memberIds/tagIds等）
  - `action_time_logs` → `events/{eventId}/actionTimeLogs/` サブコレクション（Event全体のログとして管理）
  - 移行戦略: schemaVersion確認後に一括切替（失敗時はdriftフォールバック）
- ER図HTML作成: `docs/Architecture/ER_Diagram.html`

### B-16: 設定画面UIスタイル枠修正（T-387a/b・T-388・T-389）
- 修正ファイル（4件）:
  - `member_setting_detail_page.dart`
  - `tag_setting_detail_page.dart`
  - `trans_setting_detail_page.dart`
  - `action_setting_detail_page.dart`
- 変更内容: `OutlineInputBorder` → `InputBorder.none`、ラベル外出し、`Divider`区切り
- BasicInfoスタイルと完全一致確認（レビュー承認）
- テスト: TC-SUI-001〜004 18件全件PASS

### タスクボード更新
- B-16: F-2: UI-14 新規追加
- INFRA-1: BLOCKEDを解除、T-345をDONEに更新
- INV-1〜4: BLOCKED維持（INFRA-1実装完了後に解除）

## 追加完了した作業（同セッション後半）

### INFRA-1: Firebase基盤整備 実装（T-346a・T-347）
- pubspec.yaml: firebase_core / cloud_firestore / firebase_auth / sign_in_with_apple / crypto 追加
- `flutter/lib/firebase/firebase_options_dev.dart` / `firebase_options_prod.dart` 作成
- `flutter/lib/repository/auth_repository.dart` abstract class 作成
- `flutter/lib/repository/impl/firebase/firebase_auth_repository.dart` 実装
- `flutter/lib/repository/impl/fake/fake_auth_repository.dart` テスト用実装
- main.dart: FLAVOR判定・Firebase初期化・Anonymous Auth自動サインイン
- di.dart: FLUTTER_TEST判定でFakeAuthRepository切替
- .gitignore: firebase_options_*.dart / GoogleService-Info.plist を除外
- `docs/Environment/firebase_setup.md` 作成（環境再構築手順）
- レビュー承認済み

### Firebase Console設定（ユーザー手動実施済み）
- michimark-dev / michimark-prod プロジェクト作成
- iOS アプリ登録・GoogleService-Info.plist 配置
- Anonymous Auth / Apple Sign In 有効化
- Xcode Build Phase: FLAVORに応じたplistコピースクリプト追加

### タスクボード追加
- F-3: 作業登録トピック（T-401〜T-405）追加

## 未完了

### INFRA-1: テストコード・テスト実行（T-346b・T-348）
- Firebase実機接続が必要なためUnit Test（fake_cloud_firestore）での対応を検討
- 次回セッションで方針決定

### INV-1〜4: 招待機能
- INFRA-1実装完了のためブロック解除可能
- 次回セッションでBLOCKED → TODOに変更して着手

## 次回セッションで最初にやること

1. **INV-1〜4 ブロック解除**: タスクボードのINV-1〜4をTODOに変更
2. **F-2 期間集計機能**: ユーザーと伴奏して要件定義（T-390）
3. **F-3 作業登録トピック**: ユーザーと伴奏して要件定義（T-401）
4. **UI-14 MichiInfoタイムライン道路イメージ**: デザイン提案（T-395）
5. **INFRA-1 テスト**: Unit Test方針を決めて実装（T-346b）
