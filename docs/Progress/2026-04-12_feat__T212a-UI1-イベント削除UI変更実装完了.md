# T-212a UI-1 イベント削除UI変更 実装完了

- 日付: 2026-04-12
- タスク: T-212a
- ロール: flutter-dev

---

## 完了した作業

### event_list feature
- `EventListDeleteRequested` Event を撤去
- `EventListBloc._onDeleteRequested` ハンドラを撤去
- `event_list_page.dart` の `flutter_slidable` import を除去
- `_EventListBody` から `SlidableAutoCloseBehavior` を除去し `ListView.separated` を直接返すよう変更
- `_EventListItem` から `Slidable` / `ActionPane` / `SlidableAction` を除去
- `_EventListItem` に `Key('eventList_item_${item.id}')` を付与

### event_detail feature
- `EventDetailDeleteButtonPressed` / `EventDetailDeleteConfirmed` / `EventDetailDeleteDialogDismissed` Event を追加
- `EventDetailDeletedDelegate` を `EventDetailDelegate` sealed class に追加
- `EventDetailLoaded` に `showDeleteConfirmDialog` フィールド（`bool`、デフォルト`false`）を追加
- `EventDetailBloc` に `_onDeleteButtonPressed` / `_onDeleteConfirmed` / `_onDeleteDialogDismissed` ハンドラを追加
- `EventDetailPage._handleDelegate` に `EventDetailDeletedDelegate` の case を追加（`context.pop()`）
- `_EventDetailScaffoldInner._buildAppBar` 両AppBarに削除アイコンボタン（`Key('eventDetail_button_delete')`）を追加
- `_EventDetailScaffoldInnerState` に `_showDeleteConfirmDialog` メソッドを追加（ダイアログキー・ボタンキー Spec完全一致）
- `MultiBlocListener` に `showDeleteConfirmDialog` フラグ監視 BlocListener を追加

## 未完了

- T-213 レビュー（T-212b テストコード完了後）
- T-214 テスト実行（T-213 承認後）

## 次回セッションで最初にやること

- T-213 reviewer としてレビュー（T-212b 完了後）
